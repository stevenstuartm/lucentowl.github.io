# Azure Study Guides Refinement Plan

Tracks the review-and-refine pass over all guides in `_guides/infrastructure/azure/`. Guides are consumed sequentially, in the order they appear in `assets/data/study_guides_config.json` (the "Azure" category) — that order already encodes the fundamentals-to-advanced learning path, so no guide should add its own prerequisite framing or cross-links to other Azure guides; the config ordering handles that.

Standards in force for every edit: [`.claude/skills/refine-prose/writing-standards.md`](.claude/skills/refine-prose/writing-standards.md) (always) and [`.claude/content/study-guide-guide.md`](.claude/content/study-guide-guide.md) (format/tagging/content philosophy).

## Review checklist (apply to every guide)

**Order of operations:** run item 1 (correctness & completeness) **first** — it is the only pass that adds or rewrites content, and every later check has to operate on the corrected text, not the original. Items 2–7 then refine that content; the two front-matter items (8 and 9) run last so they confirm tags and description against the final content.

1. **Factual correctness & completeness (verify against authoritative sources) — do this first.** Distinct from item 3's *pedagogical* gaps: this checks whether the guide's technical claims are actually true and current, and whether a materially important part of the topic is missing — not treating the prose as given. Web-research the guide's falsifiable claims against authoritative docs (primarily [Microsoft Learn](https://learn.microsoft.com){:target="_blank" rel="noopener noreferrer"}; official Azure product docs): service limits, character/naming constraints, defaults, support matrices (e.g., "supports move" / "does not support move"), resource-type abbreviations, feature availability and GA/preview status, pricing-tier boundaries, and any hard number or absolute ("max 24 characters", "not supported", "always inherits", "only in the same region"). Prioritize claims that are (a) falsifiable, (b) consequential if a reader acts on them, or (c) prone to drift as Azure evolves — don't spend the pass rubber-stamping prose that merely reads plausibly, and don't try to re-verify inherently stable conceptual framing. Correct stale or wrong content in place. Where a claim can't be confirmed against a source, soften it to what's verifiable or flag it in notes rather than leaving an unverified absolute standing. **Completeness is judged at study-guide altitude, not doc-completeness** — flag only a missing piece a practitioner would reasonably expect given the guide's stated scope/description, not every edge case. Because this pass can add or modify content, run the remaining checks over whatever it produces. Note only what stays unresolved (see Notes discipline below) — the corrections themselves are in the diff.
2. **Redundancy** — same fact, table, or explanation repeated across sections; consolidate or cross-reference within the same file.
3. **Gaps** — missing explanation of a concept before it's prescribed; missing trade-offs; missing "why would I not use this." (Pedagogical completeness — whether concepts are introduced in a learnable order — as opposed to item 1's factual completeness.)
4. **Clarity** — dense prose that a comparison table or list would serve better; inconsistent structure vs. the guide's own sections.
5. **ASCII diagrams** — reserve for genuinely technical relationships and flows: network topology, request/traffic flow, data flow between components, reconciliation loops, hub-and-spoke topologies, auth/token exchange sequences. The bar is whether it depicts actual structure (branching, parallel components, directional flow between distinct systems) that prose or a table can't already convey cleanly. Do **not** use a diagram to dress up a linear conceptual hierarchy, a scope/abstraction ladder, or anything that's really just a sequential list with arrows between prose labels — that's not practical output, it's decoration. If a sentence already says it clearly, don't diagram it. Skip where a table already conveys the comparison clearly.
6. **Decision trees** — where a guide has 2+ comparison tables that all feed into "which option do I pick," consider consolidating into one ASCII decision tree.
7. **Organizational/scope tier clarity.** When a guide introduces an Azure resource or concept whose behavior or constraints depend on where it lives in Azure's scope hierarchy (tenant → management group → subscription → resource group → resource, or the networking hierarchy of region → VNet → subnet → NIC), state that scope explicitly and early rather than leaving the reader to infer it from a buried constraint bullet. Example of the gap: `azure-vnet-architecture.md`'s Application Security Groups section lists "All NICs in an ASG must be in the same VNet" as a constraint, but never states up front that ASGs are VNet-scoped — a reader can't tell at a glance whether ASGs are a subscription-wide construct or something narrower. Apply this only where scope is genuinely ambiguous or consequential for how the reader would design/deploy something — not as boilerplate on every resource mentioned (e.g., it's obviously irrelevant for a concept that's inherently global or where the guide already makes scope unambiguous in context).
8. Confirm front matter (tags, description) still matches content after edits.
9. **Front matter tag audit.** Tags are free-form (`content-filter.js` / `guides-browser.js` read them straight off front matter, no fixed enum), so there's no technical constraint keeping them generic. Across the current 61 Azure guides, `azure` appears on 100%, `practical` on ~48, `infrastructure` on ~45, `cloud-computing` on ~35 — tags that common can't help anyone find a specific guide. Policy going forward:
   - Drop `azure` — redundant with category, zero discriminating value.
   - Treat `infrastructure` / `cloud-computing` as filler for this category; replace with real content signal unless a guide has nothing more specific to offer.
   - Keep exactly one skill-level tag (`fundamentals` / `practical` / `advanced`) — that's a genuine navigation axis, not filler.
   - Fill the remaining slots with the actual services/patterns/products the guide covers — the specific nouns a reader would type if they remember *what* but not *where* (e.g., `aks`, `keda`, `gitops`, `service-mesh`, `workload-identity`, `cosmos-db`, `private-link` — not just the vocabulary-table generics). Cross-check against the guide's own description; if the description already names something specific the tags don't, that's a signal the tags are under-specified.
   - Target 5-7 tags total — replace low-value generic tags rather than appending specific ones on top.
   - Review the description too: confirm it still accurately reflects content after edits and isn't just a restatement of the title.

Explicitly **not** in scope: prerequisite callouts, "where this fits" framing, or inline links to sibling Azure guides — the config-driven reading order already serves this purpose. This applies retroactively too: if a guide already has an inline link to another guide in `_guides/infrastructure/azure/`, remove it during this pass (keep the surrounding prose, drop the link/parenthetical). This does not apply to external links (Microsoft Learn docs, third-party tools) — those stay per normal inline-linking standards.

## Process

- One guide at a time (no parallel dispatch). The status column is the single source of truth for where we are.
- Per guide: run the checklist, apply the fixes, run `/refine-prose`, set status to Complete, move to the next row. Don't stop for approval between rows. (No Jekyll build — content-only edits don't break the build.)

- **Don't write commentary, in Notes or in chat, beyond questions for the user.** Notes carry forward only what a later row needs: unresolved flags, pre-flags for another row, cross-guide constraints. The corrections are in the diff. Rows 1-9 are the anti-pattern, not the template.

### Standing gotchas

- **Sibling links hide in two forms.** Grepping `](/study-guides/` finds only markdown links; links inside HTML callout blocks use `<a href="/study-guides/...">` and will be missed. Check both. Known remaining HTML-form instances: `azure-virtual-machines.md` (row 14) and `azure-managed-disks-files.md` (row 19). Some cross-references are also unlinked prose ("covered in the X guide") — those are "where this fits" framing and go too.
- **The factual pass leaves no trace in a diff.** A guide whose tags/links/diagrams look done has not necessarily had item 1 run on it. Status here, not the diff, is the record of whether item 1 ran.
- **Check the tier/zone pivot, not the default render.** Many Azure docs are zone-pivoted between a legacy tier and the current one, and the two zones can describe architecturally different products. Confirm which pivot a claim came from. The FAQ often states a transition the concept pages don't.
- **Run the linter even when the edit felt clean.** Heavy prose additions reliably introduce em-dashes and mis-ordered sections that are invisible while writing.
- **`azure.microsoft.com` pricing pages time out; the Retail Prices API doesn't.** Rows 8, 10, and 11 all lost the pricing page to timeouts. `https://prices.azure.com/api/retail/prices?$filter=serviceName eq '<Service>' and armRegionName eq 'eastus' and type eq 'Consumption'` is public, unauthenticated, returns JSON, and is the same official rate data. Use it before flagging any pricing claim unverifiable. It does have a failure mode: meters can be pre-registered for features that aren't shipped (row 11 found self-hosted-gateway meters on v2 tiers that three doc pages say don't exist), so it settles *prices*, not *feature availability* — cross-check the latter against docs.
- **Per-tier SLA percentages are not on MS Learn.** They live only in the SLA-for-Online-Services .docx, which WebFetch can't read, and the product docs deliberately say only "an SLA exists" and "zones/regions raise it." Don't assert 99.9x figures from memory; state has-SLA/no-SLA and link the SLA doc (row 11's pattern).
- **A pre-flag is a lead, not a finding.** Row 9 pre-flagged row 10 with "App Gateway is the gRPC answer, it supports HTTP/2 to backends." It is not and it doesn't, and row 9's own guide had already published the advice. Taking the pre-flag as established would have propagated the error into a second guide instead of catching it. Re-verify every pre-flag against a source before acting on it, and when one turns out wrong, fix the guide that raised it too.
- **A table column that merges two SKUs or two limits hides a fact.** Row 12 found both forms in one guide. `VpnGw1/1AZ` as a single row concealed that the non-AZ half is retiring in 2.5 months, and a single "P2S Connections" column concealed that SSTP and IKEv2/OpenVPN are independent ceilings with an 8x gap. When a table cell reads `X/Y` or names a category rather than a metric, check whether MS's own table splits it. The merge is usually where the stale or wrong number is hiding.
- **Verify code samples as claims, not as illustration.** Rows 1-27's item-1 pass checked prose assertions. Row 28's worst errors were in C#: a method overload that doesn't exist, three wrong entity APIs, a non-generic call assigned to a variable, and a return-type mismatch. A sample that reads plausibly can still be uncompilable, and readers copy samples more literally than they follow prose. For any guide with code, check attribute names, method names, overload signatures, and return types against the current API reference. Guides with heavy code ahead: rows 29-31 (Bicep/ARM), 45-47 (pipelines/Actions), 48 (ML), 56-57 (serverless/Functions advanced), 59 (container patterns).
- **A guide can document a feature that does not exist.** Rows 1-26 found stale numbers, wrong limits, and reversed claims, all of which start from something real. Row 27 found an entire subsection (with a config block, a characteristics list, and a row in the AWS comparison table) for Event Grid "strict ordering per subject," which the product has never had. The tell was that the docs never mention it, not that they contradict it. When verification returns *nothing* about a named feature rather than something different from what the guide says, that absence is the finding. Search for the setting name itself before assuming the docs just cover it elsewhere.
- **Item 5 is a two-way check, not a filter.** Rows 1-9 all ran it in one direction: judge the diagrams already present, drop the decorative ones, add nothing. That is half the check, and it lets a guide pass while every relationship it teaches stays in prose-and-table form. Row 7 was reopened for exactly this. Ask both questions on every guide: does each existing diagram clear the bar, **and** does the guide explain a structure with no diagram? The second finds more than the first. Structures that qualify and are easy to miss: a control that enforces at two levels, two options whose traffic paths differ in shape (not just in attributes), and a topology whose behavior comes from routing rather than from the links drawn. **Rows 1-9 have not had the second question asked** — worth a sweep once the first pass is done.

## Progress

| # | Subcategory | Guide | Status | Notes |
|---|---|---|---|---|
| 1 | Architecture Principles | azure-well-architected-framework.md | Complete | **Verified vs. MS Learn:** Functions timeouts/plan status, reservation & savings-plan discount ceilings, Azure SQL and Cosmos DB SLAs. **Corrected:** "10-minute default timeout" was wrong twice — Consumption is 5 min default / 10 max; guide now names Flex Consumption (30 min default, unbounded max), since MS marks Consumption legacy for new apps. Dropped the "<10 minutes → Functions" decision rule (rested on the wrong number; Functions-vs-Container Apps now split on programming model / image control). "40-72% savings" → up to 72% (reservations) / up to 65% (savings plans); the 40% floor wasn't an MS figure and the two products' ceilings differ. **Verified correct, left alone:** SQL 99.995% (zone-redundant Business Critical), Cosmos 99.999% (multi-region writes). Items 2-7 no-ops: no diagrams warranted (conceptual guide), no sibling links present, two comparison tables answer separate questions so no decision tree. Tags/description already compliant. |
| 2 | Identity & Access Management | azure-entra-id.md | Complete | **Verified vs. MS Learn:** Entra licensing table, Cloud Sync vs. Connect feature comparison, B2C/External ID product status, Connect sync interval. **Corrected:** (a) SSPR row said Free = No; SSPR is included in Free for cloud-only users, P1 is what adds on-prem writeback. (b) "Entra External ID (formerly Azure AD B2C)" was wrong — External ID is a separate next-gen product, not a rename; B2C closed to new customers 2025-05-01 and is supported to at least May 2030. Consequential for anyone picking a CIAM platform today. (c) Cloud Sync "does not yet support group writeback" is wrong — group writeback V1 is supported in both, and cloud-to-AD group provisioning is Cloud-Sync-only; replaced with the real gaps (Hybrid Join device sync, 150K objects/domain and 50K members/group ceilings, advanced sync rules, cross-forest refs) and noted MS now treats Cloud Sync as strategic direction. (d) "Entra ID Connect" is not a real product name — replaced with "Microsoft Entra Connect" throughout (6 spots). **Added:** short note that Entra ID Governance is now a separate SKU holding entitlement management / lifecycle workflows / newer access reviews, since the P2 table alone now understates the picture. **Verified correct, left alone:** Conditional Access = P1, dynamic groups = P1, ID Protection + PIM = P2, Connect default 30-min sync. Items 2-7 no-ops: scope-hierarchy para already present; auth flows read cleanly as tables/lists so no diagram clears item 5's bar; tables answer separate decisions so no decision tree; no sibling links. Tags/description already compliant. |
| 3 | Identity & Access Management | azure-rbac-managed-identities.md | Complete | **Verified vs. MS Learn:** deny-assignment creation paths, Azure Blueprints status, built-in role count, PIM licensing, IMDS endpoint. **Corrected:** (a) "Deny assignments are created by Azure Blueprints" is stale — MS's current deny-assignments doc doesn't mention Blueprints at all; deployment stack deny settings are the mechanism now. Rewrote and noted Blueprints is retiring (phased from 2026-07-31, complete 2027-01-31, deployment stacks + template specs as replacement) so readers who find Blueprints deny assignments in an existing estate know to migrate. (b) PIM licensing said "P2" — it's P2 **or** Entra ID Governance, and the license is per eligible user/approver/reviewer, which is the part that bites on cost. **Softened:** "over 300 built-in roles" (3 spots) → "hundreds" — MS docs no longer publish a count, so the number is unverifiable; it's a safe lower bound but drift-prone and the exact figure changes nothing for the reader. **Verified correct, left alone:** IMDS 169.254.169.254, system vs. user-assigned lifecycle semantics, client secret 2-year portal max. Items 2-7 no-ops: the two token-flow diagrams clear item 5's bar (auth/token exchange between distinct systems, explicitly greenlit); tables serve separate decisions so no decision tree; no sibling links. Tags/description already compliant. |
| 4 | Subscription & Resource Organization | azure-subscription-architecture.md | Complete | **Verified vs. MS Learn:** management group depth/count limits, the whole quota table, cross-tenant VNet peering, resource group location semantics. **Corrected:** (a) "six levels deep (root plus five additional levels)" — MS states six levels *not counting* root or subscription, so it's root plus six; added the 10,000 MGs per directory limit. (b) "Azure SQL Databases: 500 per server" is off by 10x — it's 5,000 per logical server; added the separate 250 logical servers per subscription per region limit that was missing. (c) "Public IP Addresses: 1,000" is only the Enterprise Agreement default — pay-as-you-go defaults to **20**, which is the kind of thing that blindsides someone mid-deployment. (d) Table header claimed "per subscription per region" for every row while the SQL row was per-server; moved scope qualifiers into the rows. (e) Internal contradiction: L86 said VNet peering works cross-tenant, L218 said "within the same tenant" — reconciled to cross-tenant supported with trust config. **Verified correct, left alone:** storage 250/region (500 by request), NSGs 5,000, VNets 1,000, RG location = metadata only. Items 2-7 no-ops: the four hierarchy trees show real branching so they clear item 5's bar (the earlier pass had already stripped the ones that were just lists); Decision Framework already sequences the choices so a decision tree would duplicate it; no sibling links. Tags/description already compliant. |
| 5 | Subscription & Resource Organization | azure-resource-organization.md | Complete | **Verified vs. MS Learn:** every resource-type abbreviation against the CAF list, AKS/App Gateway/Firewall/ExpressRoute move support, tag limits, storage/Key Vault name constraints. **Corrected:** (a) App Service Plan abbreviation `plan` → **`asp`**. (b) Load Balancer `lb` → **`lbi`/`lbe`** — CAF has no plain `lb`, it splits internal/external. **Added (completeness gap):** a "Tag Limits and Constraints" subsection — the guide recommended a 9-tag taxonomy but never stated the ceiling (50 pairs per resource; 15 for Automation/CDN/DNS/LA saved searches), the 512/256 char limits (128 for storage account tag names), that tags stop at subscription level (management groups don't support them — an item-7 scope point), the forbidden name characters, and that tags are plain text and surface in cost reports/exports, so no secrets. **Verified correct, left alone:** AKS genuinely cannot move between resource groups, subscriptions, or tenants (AKS FAQ is explicit — the guide was right); storage 24-char/lowercase; Key Vault 24-char; tag names case-insensitive / values case-sensitive; tags don't inherit. Items 2-7 no-ops: no diagram warranted (hub-and-spoke is only mentioned in passing here — it belongs to row 7); reference tables don't feed a single "which do I pick" so no decision tree; no sibling links. Tags/description already compliant. |
| 6 | Subscription & Resource Organization | azure-billing-enrollment.md | Complete | First genuinely untouched guide — needed the tag audit and link removal too. **Verified vs. MS Learn:** EA portal status, reservation scopes, Dev/Test eligibility, MCA hierarchy, spending limits. **Corrected:** (a) "EA Portal (being migrated to Azure portal)" — `ea.azure.com` was **retired in Feb 2024** and is read-only; described as in-progress when it finished two years ago. (b) Reservation scope listed 3 options and the bullets only explained 2 — there are **four**, and the missing one (management group) is the useful middle ground; also added that scope is changeable post-purchase, which turns an underutilized reservation into a fixable problem. (c) "Enterprise Agreement customers have access to Dev/Test pricing" — not EA-only; MCA has Azure Plan for Dev/Test via Microsoft sales, and Visual Studio subscribers get a PAYG Dev/Test offer. (d) MCA framed as "small to mid-size" in prose and the comparison table — MCA spans web-direct up to enterprise agreements negotiated through Microsoft sales; softened both. **Items 5:** dropped the PAYG and CSP "hierarchy" diagrams — both were linear 3-4 node chains with no branching, i.e. item 5's decoration case — and replaced with prose that carries the actual point. Kept the MCA and EA trees, which show real branching. **Items 8-9:** tags were `[infrastructure, azure, cost-analysis, governance, fundamentals]` → `[enterprise-agreement, mca, csp, reservations, savings-plans, cost-allocation, fundamentals]`; dropped 2 sibling links (L12 subscription-architecture, L178 resource-organization) plus an unlinked "covered in the Resource Organization guide" parenthetical. **Flagged, unresolved:** could not find a definitive MS source on whether EA is still sold to new customers vs. all enrollments moving to MCA-E at renewal. The guide still presents EA as the enterprise endpoint. Softened MCA rather than assert a transition I couldn't source — worth a targeted check if a source turns up. |
| 7 | Networking & Content Delivery | azure-vnet-architecture.md | Complete | Biggest haul so far; the NAT Gateway error was the most consequential found in the pass. **Verified vs. MS Learn:** NAT Gateway SKUs/zones/throughput/SNAT, default outbound retirement mechanism, cross-zone data transfer pricing, subnet reserved IPs, SQL MI /27, Bastion /26. **Corrected:** (a) **NAT Gateway "zone-redundant by default" is wrong** — Standard SKU is *zonal*; deployed without a zone it's *nonzonal* and MS explicitly recommends against it because a zone outage kills outbound for every attached subnet including VMs in healthy zones. Only StandardV2 is zone-redundant. The claim appeared twice, including inside the multi-zone pattern, so following the guide would have put a single zonal failure point inside a "zone-redundant" design. Added a SKU comparison table and the no-in-place-upgrade caveat. (b) "50 Gbps per public IP" — it's per *gateway resource* (Standard 50 split 25/25; StandardV2 100). (c) SNAT ports "64,000 per IP per destination" → 64,512 per public IP (the per-destination number is 50,000 concurrent connections, a different limit). (d) Default outbound "starting in late 2025" — actual mechanism is API versions after **2026-03-31** default *new* VNets' subnets to private; existing VNets are untouched and old pinned API versions keep the old behavior. Rewrote the section and Pitfall 6. (e) **Pattern 2's diagram contradicted the guide's own prose** — it drew subnets nested inside zones (the AWS model) while the paragraph below correctly says Azure subnets span zones. Redrew it. **Item 7 (the plan's own worked example):** ASG VNet-scoping now stated up front, with what it bounds in a hub-and-spoke estate, instead of only appearing as a buried constraint bullet. **Verified correct, left alone:** no cross-zone data transfer charges (MS reaffirmed May 2024), 5 reserved IPs, default NSG rules/priorities, service endpoint routes overriding UDRs. **Items 8-9:** tags `[infrastructure, azure, networking, security, scalability, practical]` → `[vnet, nsg, nat-gateway, private-endpoints, hub-spoke, vnet-peering, practical]`; removed 4 sibling links (firewall-ddos in an HTML callout — note grep for markdown links misses those, private-link-virtual-wan ×2, expressroute-vpn). **Item 5 reopened** (the original pass only filtered, never added): dropped the Pattern 1 and Pattern 4 chains — linear, no branching, the same shape row 6 cut — and added four diagrams: the subnet/NIC attachment model, NSG evaluation order, service-vs-private-endpoint traffic paths, and spoke-to-spoke via the hub firewall. Patterns 2 and 3 kept. |
| 8 | Networking & Content Delivery | azure-dns-traffic-manager.md | Complete | **Verified vs. MS Learn:** alias record targets, Multivalue max return, Azure DNS SLA/pricing model, DNSSEC GA date, private DNS + Private Resolver limits, TM monitoring/endpoint/nesting rules, private endpoint zone names. **Corrected:** (a) **Alias targets were missing two of five** — Front Door endpoints and same-zone record sets are both supported. Pitfall 5 was built on the error: it named Front Door as unsupported and prescribed putting Traffic Manager in front of it, i.e. a needless hop and bill for an alias Azure accepts directly. Rewrote the pitfall around the constraint that actually exists. (b) **"Private DNS zone queries are free" is wrong** — zones *and* queries are billed for both public and private (MS moderator + FAQ billing model; the pricing page itself timed out repeatedly, see flag). (c) Multivalue "up to 8" → configurable max return count, default 2, ceiling 10. (d) TM labeled "DNS (Layer 3/4)" — MS states TM works at DNS, which is application layer; the label implies packet/connection-level routing TM never does. Reframed the table row around mechanism. (e) Azure Monitor listed one private DNS zone; AMPLS needs five, and missing one silently leaves telemetry on public endpoints. Generalized the "one zone per service" framing, which was the root error. (f) Endpoint table listed "Cloud Services"; replaced with App Service slots + the three constraints that actually break configs (public IP needs a DNS name, Web Apps need Standard+, one Web App per region per profile). (g) Geographic "default endpoint" isn't the mechanism — it's assigning the **World** region; also added that Geographic returns the mapped endpoint *regardless of health*, so it has no failover by construction. Rewrote Pitfall 3. (h) TTL table's "Failover Speed" column contradicted the corrected failover math; it measures propagation only. **Added (completeness):** A/AAAA-alias-to-TM requires external endpoints with static IPs (bites exactly at the apex, the guide's headline use case); the all-endpoints-Degraded best-effort behavior (a misconfigured profile is indistinguishable from a healthy one until failover is needed); probes need the `AzureTrafficManager` service tag; DNSSEC is public-zones-only and `168.63.129.16` doesn't validate; autoregistration-disable permanently deletes records; external-endpoint health-check billing survives service deletion. **Item 7:** private DNS zones stated as **global** resources up front; Private Resolver stated as regional/single-VNet, which is what makes hub placement the answer. **Items 5-6 no-ops:** the Private Resolver diagram shows real directional flow between distinct systems, so it stays; CNAME chain reads fine as a numbered list; TM-vs-Front-Door already has "use X when" lists, so a decision tree would duplicate. **Items 8-9:** tags `[infrastructure, azure, networking, dns, scalability, reliability, practical]` → `[azure-dns, traffic-manager, private-dns-zones, dns-private-resolver, private-endpoints, alias-records, practical]`; description still accurate, left alone. No sibling links present (checked both markdown and HTML forms). Linter clean; also split 4 pre-existing semicolons gluing causal clauses. Jekyll build passes. **Flagged, unresolved:** `azure.microsoft.com/pricing/details/dns/` timed out on every attempt, so the private-query billing correction rests on the Azure DNS FAQ's billing model plus a MS moderator Q&A rather than the pricing page itself. The direction is not in doubt (queries are billed), but no per-unit figures were asserted — worth a confirming look if the page loads later. |
| 9 | Networking & Content Delivery | azure-front-door-cdn.md | Complete | **Flagged, unresolved:** WAF custom rule priority range unverifiable — MS documents only "a unique integer," so the guide's "1-100" was softened rather than asserted. **Cross-guide:** Front Door Standard/Premium uses unicast POP selection via an internal Traffic Manager profile (MS switched Mar-Apr 2026); anycast is classic-only. Any other guide describing Front Door routing must match this. |
| 10 | Networking & Content Delivery | azure-load-balancer-app-gateway.md | Complete | **Row 9's pre-flag (b) was wrong, and row 9's guide acted on it — now corrected there.** Standard App Gateway also speaks **HTTP/1.1 to backends**; HTTP/2 is client-side only and TLS-only (h2c → 403). So gRPC fails behind App Gateway for exactly the reason it fails behind Front Door. `azure-front-door-cdn.md` told readers to "Use Application Gateway" for gRPC; rewritten in place to AGC (native gRPC to backends) or a pass-through L4 path. **Cross-guide: the gRPC answer is AGC, never App Gateway.** Pre-flags (a) and (c) held, no reverse error present; App Gateway WAF group-by is ClientAddr/GeoLocation/None plus XFF variants, and its window is **sliding** where Front Door's is fixed. **Pre-flag row 13** (private-link-virtual-wan): internal LB *is* a Private Link service — this guide's table wrongly said "N/A". **Pre-flag row 34** (firewall-ddos): Gateway Load Balancer is the chaining-based NVA insertion product (VXLAN, flow symmetry, no UDRs), and DDoS Network Protection (~$2,944/mo) rebills WAF_v2 at Standard_v2 rates. **Flagged, unresolved:** the Load Balancer pricing page timed out on every attempt, so no LB per-unit figures are asserted. The cost comparison rests on App Gateway's own published rates ($0.246/hr Standard_v2, $0.443/hr WAF_v2) and calls LB "an order of magnitude" less without sourcing a multiple; this replaced the guide's unsourced "5-7x", which was itself an understatement. |
| 11 | Networking & Content Delivery | azure-api-management.md | Complete | **Pricing pages time out, but `prices.azure.com/api/retail/prices` does not.** The public Retail Prices API returns official per-meter figures and settled the Premium-vs-Standard-v2 ratio this guide asserts (Premium $3.829/hr vs Standard v2 $0.9589/hr = 4x, so the existing "one-quarter" claim held). Use it for rows 12-61 rather than flagging pricing unverifiable — it would have answered rows 8 and 10 too. **Flagged, unresolved:** per-tier SLA percentages are not published on MS Learn, only in the SLA-for-Online-Services .docx, and `azure.microsoft.com` timed out on every attempt (3rd row running). Replaced the guide's 99.95/99.99 column with has-SLA/no-SLA plus a pointer to the SLA doc. **Conflict, resolved toward docs:** the Retail Prices API lists "Standard v2 / Premium v2 Self-hosted Gateway" meters, but three separate doc pages say the self-hosted gateway is Developer + Premium (classic) only. Treated the meters as pre-registered and followed the docs. If a later row needs self-hosted gateway on v2, re-verify rather than trusting either source. **Cross-guide:** APIM gateway-to-backend HTTP/2 is classic-only and in preview (instances created from Jan 2026); pass-through gRPC is likewise classic-only. Consistent with row 10's "the gRPC answer is AGC" — nothing to reconcile. **Pre-flag row 13** (private-link-virtual-wan): APIM inbound private endpoints are supported on classic Basic/Standard/Premium and on Standard v2/Premium v2, but **not** Basic v2 — the one v2 tier without them. **Pre-flag row 35** (key-vault): with the Key Vault firewall on, APIM must use its **system-assigned** identity (user-assigned is rejected) and needs "Allow trusted Microsoft services to bypass this firewall". **Pre-flag row 34** (firewall-ddos): v2 tiers cannot enable Azure DDoS Protection. |
| 12 | Networking & Content Delivery | azure-expressroute-vpn.md | Complete | **The SLA gotcha held for a third product, and the pricing API wasn't needed** (no per-unit figures asserted). MS Learn publishes no VPN/ExpressRoute SLA percentages anywhere — the HA page, the FAQ, and the intro all link the SLA doc and stop. Replaced the guide's 99.95/99.99 column with has-SLA plus the docs' own resiliency vocabulary (single circuit = "standard resiliency", two circuits at two peering locations = "maximum resiliency"). **Corrected:** (a) **"Basic SKU lacks IKEv2" is wrong** and appeared twice — Basic route-based S2S *is* IKEv2; what Basic lacks is IKEv2/OpenVPN for P2S, which is why its remote access is SSTP-only/Windows-only. Pitfall 5's "no IKEv2 means weaker security" rested on the error. (b) **The single "P2S Connections" column conflated two independent ceilings** — SSTP is Max 128 on *every* SKU including VpnGw5AZ; the 250/500/1,000/5,000/10,000 figures are IKEv2/OpenVPN only, and MS states they're concurrent. Sizing an SSTP deployment by climbing the SKU ladder buys nothing, and the guide implied 8x headroom that doesn't exist. Split into two columns. (c) **GatewaySubnet /27 is required, not "recommended"** — MS: "/29 (Basic only), all other SKUs require /27 or larger," and the coexistence doc enforces it. Pitfall 1's premise (a /28 that works until you add a gateway) was wrong; rewrote around what actually happens. (d) **Non-AZ VpnGw1-5 retire 30 Sept 2026** — 2.5 months out; uncreatable since 1 Nov 2025 and existing ones already reject config changes. The guide's "VpnGw1/1AZ" paired rows presented a dead SKU as a live option. Rebuilt both tables on AZ SKUs only. (e) ExpressRoute Direct is **10/100/400 Gbps**, not 10/100 (3 spots incl. the AWS comparison's "max private bandwidth"). (f) FastPath IP limits were missing the 10 Gbps Direct tier (100,000). (g) Pattern 1's "Azure natively prefers ExpressRoute" is incomplete — longest-prefix-match runs first, so a more specific VPN prefix wins. **Added (completeness):** FastPath's spoke/UDR/Private-Link/IPv6 features are **ExpressRoute Direct only** — a provider circuit gets the hub VNet over IPv4 and nothing else, for an ErGw3Az-tier gateway; ER↔VPN transit needs **Azure Route Server** + ASN 65515 (new Pitfall 4 — BGP alone never does it); **Global Reach is unavailable on Local** and its links consume the circuit's VNet-connection budget; M365 over ER **requires Premium**; BFD cuts ER failure detection from ~3 min to <1s; ErGwScale doesn't support IPsec over ER and is missing from West Europe/Japan East/SE Asia/South Central US/East US 2; gateway route-learning (4,000 on Standard) is a separate ceiling from the circuit's 10,000. **Item 5 (both directions):** guide had **zero** diagrams; added three where structure was buried in prose — the 4-tunnel dual-redundancy mesh, FastPath's control/data plane split with the gateway-fallback path, and ER+VPN coexistence traffic paths. **Item 6:** added a circuit-SKU decision tree (Local/Standard/Premium was spread across a table and three sections, and M365/Global Reach override region scope). **Item 7:** circuit scope stated up front — a circuit lives at a *peering location*, not a region, and crosses subscription/tenant via authorizations. **Items 8-9:** tags `[infrastructure, azure, networking, security, reliability, practical]` → `[expressroute, vpn-gateway, hybrid-connectivity, global-reach, fastpath, bgp, practical]`; removed 1 sibling link (L111 vnet-architecture); description still accurate. Linter clean, Jekyll build passes. **Pre-flag row 55** (hybrid-cloud-architecture): ER↔VPN transit requires Route Server — if that guide describes hybrid transit, verify it says so. **Pre-flag rows 60/61** (multi-region, DR): "maximum resiliency" = two circuits at two peering locations is the current MS vocabulary, and no SLA percentage is publishable from Learn. |
| 13 | Networking & Content Delivery | azure-private-link-virtual-wan.md | Complete | **Item 1 ran, no factual errors found** (first Azure row that was already accurate). Verified vs. MS Learn: all five Private DNS zone names (blob/database/vaultcore/documents/azurewebsites — correct), VWAN Basic-vs-Standard feature table (correct), cross-region + cross-tenant private endpoints, PLS = Standard LB (no internal-only requirement — row 10's pre-flag was about row 10's own table; this guide was already consistent). **Corrected only:** the hub-router throughput line implied 50 Gbps out of the box — it's the scaled ceiling (default 2 routing units = 3 Gbps, scales to 50 units = 50 Gbps; 10,000-route cap is independent of capacity). **Item 5 (guide had zero diagrams):** added two — Service-Endpoint-vs-Private-Endpoint traffic paths (the two-shapes case: public IP vs private-IP-in-subnet), and a secured VWAN hub with routing intent (spoke-to-spoke detour through hub firewall + auto inter-hub mesh). DNS CNAME chain left as a numbered list (row 8 precedent). **Items 8-9:** tags `[infrastructure, azure, networking, security, architecture, scalability, practical]` → `[private-link, private-endpoints, virtual-wan, private-dns-zones, service-endpoints, routing-intent, practical]`; description already names specifics, left alone. No sibling links (checked markdown + HTML forms). Linter clean. |
| 14 | Compute Services | azure-virtual-machines.md | Complete | **Verified vs. MS Learn:** all five disk types' max IOPS/throughput, VM SLA tiers (99.9/99.95/99.99), AWS EC2 SLA, availability-set FD/UD caps (3/20), accelerated networking 2+ vCPU, four reservation scopes, AHB core rules. **Corrected:** (a) **Premium SSD v2 max throughput 1,200 → 2,000 MB/s** (disks-types table). (b) **Internal contradiction on single-instance SLA:** the AWS comparison table gave AWS 99.5% while the prose (Single VM SLA section + Key Takeaway 10) said "AWS does not offer an SLA for a single EC2 instance." AWS *does* publish a 99.5% instance-level SLA; reframed all three spots to "Azure's 99.9% is the higher of the two" and dropped the bogus "with EBS optimization" qualifier. (c) **AHB Datacenter "covers unlimited VMs on up to 16 cores"** is wrong for multi-tenant VMs — reframed to the real rule (min 8 core licenses/VM scaling to core count, 16 cores/processor license) and Datacenter's actual distinction (dual-use rights); noted "unlimited VMs per host" applies only to Azure Dedicated Host / AVS. **Item 5:** added a fault-domain × update-domain placement grid to the Availability Sets section (the guide described FD/UD spreading only in prose bullets); the four pattern diagrams clear the bar and stay. **Items 8-9:** tags `[infrastructure, azure, cloud-computing, scalability, reliability, cost-analysis, practical]` → `[virtual-machines, vm-scale-sets, spot-vms, availability-zones, reserved-instances, azure-hybrid-benefit, practical]`; description already specific. **Sibling links removed (3):** vnet-architecture (L12 markdown), the "Disk Guide Cross-Reference" HTML callout to managed-disks-files, vnet-architecture in Pattern 4 and in Pitfall 3. Linter clean. **Pre-flag row 19** (managed-disks-files): Premium SSD v2 max throughput is **2,000 MB/s**, Ultra is 400,000 IOPS / 10,000 MB/s — verify that guide's disk table matches. |
| 15 | Compute Services | azure-functions.md | Complete | **Headline gap (item 1 completeness): the guide never mentioned Flex Consumption**, now Microsoft's recommended serverless plan — so every "Consumption has no VNet / VNet requires Premium" claim was stale. Verified Flex vs. MS Learn (Linux-only, scale to zero and out to 1,000, VNet integration + private endpoints, per-function scaling, selectable 512 MB/2 GB/4 GB instances, optional always-ready, 30-min default timeout with no fixed max, 250-core regional quota, one app per plan) and integrated it: added a Flex column to the Plan Comparison, Network Feature, and Execution Limits tables; added a Flex Consumption subsection and reframed classic Consumption as the legacy/Windows-or-no-Flex-region option; corrected the networking callout, Pitfall 1, and Key Takeaway 7 to stop saying VNet needs Premium/Dedicated; updated the AWS Lambda hosting row and per-function-scaling note. **Verified correct, left alone:** classic Consumption 5-min default / 10-min max timeout (row 1 pre-established), Premium 30-min/unbounded, AWS Lambda 15-min hard limit, Consumption 200/100 max instances, Premium EP 3.5-14 GB. **Item 6:** added a hosting-plan decision tree (plan choice was spread across four tables). **Items 8-9:** tags `[infrastructure, azure, cloud-computing, scalability, cost-analysis, practical]` → `[azure-functions, serverless, durable-functions, triggers-bindings, flex-consumption, cold-start, practical]`; description still accurate. **Sibling links removed (6 markdown):** api-management ×2, vnet-architecture ×2, expressroute-vpn, private-link-virtual-wan. Item 5 no-op (4 pattern data-flow diagrams clear the bar; no missing structure warrants one). Linter clean. **Pre-flag rows 56/57** (serverless-architecture-patterns-azure, azure-functions-advanced): any Functions plan discussion must include **Flex Consumption** as the recommended serverless plan and must not claim VNet integration requires Premium/Dedicated — classic Consumption is the only plan without it. |
| 16 | Compute Services | azure-container-services.md | Complete | **Verified vs. MS Learn:** AKS tier pricing (Free = $0/no-SLA, Standard ≈ $0.10/hr + 99.95%/99.9% SLA, Premium ≈ $0.60/hr + LTS), 5,000 nodes/cluster, N-2 version support, ACI 60 containers/group + 100 groups per subscription per region default, EKS $73/mo. **Corrected:** (a) **AWS-comparison table said AKS control plane is "free on free and standard tiers"** — wrong and self-contradicting (the tier table and L40 both say Standard/Premium charge). Only the Free tier is free; fixed to "Standard and Premium charge per cluster hour." (b) Decision-matrix max-scale cell "300 replicas per app (consumption), higher with dedicated profiles" — the 300/1,000 split is portal-vs-CLI, not consumption-vs-dedicated; fixed to "1,000 replicas per app (300 via the portal, 1,000 via CLI/ARM)"; also added "per region" to the ACI 100-groups quota. **Added (completeness):** kubenet retirement — MS retires kubenet for AKS on **2028-03-31**; new + existing clusters must move to Azure CNI Overlay. The guide already recommended Overlay but presented kubenet as a live (if limited) option. **Item 5:** added a two-level autoscaling diagram (pod-level HPA/KEDA → scheduling pressure → node-level Cluster Autoscaler) — the "control at two levels" case the prose stated but didn't draw. **Item 6:** added a 3-way selection decision tree (full-K8s? → AKS; short-lived/burst? → ACI; else → Container Apps) consolidating the matrix + three when-lists. **Items 8-9:** tags `[infrastructure, azure, cloud-computing, kubernetes, scalability, practical]` → `[aks, container-apps, aci, kubernetes, keda, dapr, practical]`; description already names all three services, left alone. **Sibling links removed (1):** VNet Architecture (L479 markdown, subnet-sizing parenthetical). No HTML-form links. The 5 pattern diagrams clear item 5's bar and stay. Linter clean. |
| 17 | Compute Services | azure-app-service.md | Complete | **Verified vs. MS Learn:** scale-out ceilings (Basic 3 / Standard 10 / Premium v3 30 / Isolated v2 100 per plan), slot counts (Standard 5, Premium/Isolated 20), ASE v3 = 200 instances/ASE but 100 per Isolated v2 plan (guide's per-plan 100 is correct), managed-cert renewal. **Corrected:** (a) **Regional VNet integration is Basic-tier-and-above, not Standard+** — the official vnet-integration doc lists "Basic, Standard, Premium, Premium v2/v3/v4, Elastic Premium." The guide said Standard+ in **5 places** (AWS-comparison "Standard+", tier-table Basic = "No", the "Choosing a Tier" Standard para, the default-networking para "no VNet at Free/Shared/Basic", and the VNet-integration section header). Fixed all five; Standard's real delta over Basic is auto-scale + slots, not VNet. (b) **App Service Managed Certificates now support apex/naked domains** (GA since May 2021) — the guide said they support "neither wildcard nor naked/apex"; only wildcard is still unsupported. **Items 5-6 no-ops:** the 4 pattern diagrams clear item 5's bar and stay; slot-swap is a numbered sequence that reads fine; tier selection is handled by the "Choosing a Tier" prose + Key Takeaway 2, and the WebJobs-vs-Functions and SWA-vs-App-Service tables answer separate questions, so no single decision tree to build. Item 7 n/a (plan-is-the-scaling-unit already stated up front; App Service scope isn't ambiguous). **Items 8-9:** tags `[infrastructure, azure, cloud-computing, scalability, reliability, practical]` → `[app-service, app-service-plans, deployment-slots, app-service-environment, webjobs, easy-auth, practical]`; description already names plans/slots/scaling/WebJobs, left alone. **Sibling links removed (7):** vnet-architecture ×3 (L206 + L519 were pure "see the guide" pointers, dropped whole sentences), private-link-virtual-wan, front-door-cdn ×2, load-balancer-app-gateway, expressroute-vpn. Kept the intra-page `#app-service-environment-v3` anchor. Linter clean (2 em-dashes remaining are pre-existing Pattern-2 diagram labels inside a code fence, out of prose scope). |
| 18 | Storage Services | azure-blob-storage.md | Complete | **Item 1 ran, no factual errors found** (2nd already-accurate Azure guide, after row 13). Verified vs. MS Learn: block blob max 190.7 TiB (50,000 × 4000 MiB blocks; 5000 MiB single-write), page blob 8 TiB, tier minimums (Cool 30 / Cold 90 / Archive 180 days), archive rehydration (standard ≤15 h, high-pri <1 h for <10 GiB), durability nines (LRS 11 / ZRS 12 / GRS+GZRS 16), account name 3-24, container name 3-63, 5 stored access policies/container, user-delegation SAS ≤7 days, 200 firewall IP rules, 10 blob index tags, soft delete 1-365 days, premium block blob = LRS/ZRS + Hot-only, reserved capacity 100 TiB/1 PiB. **PITR prerequisites (soft delete + change feed + versioning) confirmed correct.** **Item 5 (added one):** service-endpoint-vs-private-endpoint traffic-path diagram in the Networking section — the "two shapes" difference (public endpoint over backbone vs private IP NIC in subnet) was prose-only; it also restores the self-sufficiency lost when the sibling pointer to VNet Architecture was removed. The 4 pattern diagrams clear the bar and stay; the lifecycle day-range blocks are concrete policy schedules (config, not decorative prose), kept. **Items 2/6/7 no-ops:** redundancy table + "Choosing Redundancy" prose + Decision Framework each add a distinct layer (specs / rationale / quick-map), not redundant; the Redundancy Decision Framework already serves as the decision aid so no new tree; storage-account scope (redundancy/firewall/tier/HNS all account-level) is stated up front at L54. **Items 8-9:** tags `[infrastructure, azure, cloud-computing, scalability, cost-analysis, security, practical]` → `[blob-storage, access-tiers, lifecycle-management, storage-redundancy, adls-gen2, sas-tokens, practical]`; description already names account types/tiers/lifecycle/redundancy, left alone. **Sibling links removed (3 locations, 4 links):** rbac-managed-identities (L334), vnet-architecture (L392 pointer sentence dropped, replaced by the diagram), front-door-cdn ×2 (L457). No HTML-form links. Linter clean. |
| 19 | Storage Services | azure-managed-disks-files.md | Complete | **Row 14's pre-flag held** (verified against the disks-types page, ms.date 2025-11-06): the disk-types table understated two ceilings. **Corrected:** (a) **Ultra Disk 160,000 IOPS / 4,000 MiB/s → 400,000 IOPS / 10,000 MB/s** (fixed in 4 spots: disk-types table, Ultra prose, AWS-comparison "up to 160K" → 400K, and the Storage Selection Framework "up to 160K" → 400K). (b) **Premium SSD v2 max throughput 1,200 → 2,000 MB/s** (disk-types table). Row 14's own guide already carries the correct 400K/10,000 and 2,000 figures, so nothing to fix there. **Verified correct, left alone:** Premium SSD v2 baseline 3,000 IOPS / 125 MB/s free; credit-based bursting 3,500 IOPS / 170 MB/s on P20-and-smaller; on-demand bursting on P30+; P10 = 500/100, P50 = 7,500/250, P80 = 20,000/900; Standard SSD 6,000/750; Standard HDD 2,000/500; OS disk 4 TiB, data disk 32,767 GiB; Pattern 5's Ultra example figures (80,000 IOPS / 2,000 MB/s on 1 TiB) are within caps, kept as an example not a max-claim. **Items 5-6 no-ops:** the 5 pattern diagrams (tiered layout, shared-disk cluster, file-sync topology, AKS PV, Ultra DB) clear item 5's bar; disk-type and storage-service selection are each already served by one table + per-option prose/lists (not 2+ tables feeding one decision), so no decision tree added. Item 7 n/a (LRS-disk-pinned-to-zone vs ZRS-any-zone already stated at L233). **Items 8-9:** tags `[infrastructure, azure, cloud-computing, reliability, performance, cost-analysis, practical]` → `[managed-disks, azure-files, azure-netapp-files, azure-file-sync, ultra-disk, premium-ssd-v2, practical]`; description already names disk types/shared disks/SMB+NFS/ANF, left alone. **Sibling links removed (2):** expressroute-vpn (L289 **HTML-form** `<a href>` inside the port-445 callout — the instance the standing gotcha pre-flagged), private-link-virtual-wan (L342 markdown). Linter clean. |
| 20 | Database Services | azure-database-selection.md | Complete | Selection/decision guide, table-heavy. **Verified vs. MS Learn:** SQL MI 16 TB (GP + BC, correct), active geo-replication 4 secondaries, Cosmos 5 consistency levels, Redis P5 = 120 GB / 1.2 TB clustered, non-Hyperscale SQL DB 4 TB. **Corrected (scale-limit drift):** (a) **Azure SQL Database Hyperscale 100 TB → 128 TB** (raised; 3 spots — deployment table, Synapse-vs-SQL table, big comparison table; note the Hyperscale *elastic pool* max is still 100 TB but these refer to single DB). (b) **PostgreSQL Flexible Server 16 TB → 32 TiB** (64 TiB on Premium SSD v2). (c) **MySQL Flexible Server 16 TB → 32 TiB** (Memory Optimized tier). (d) **Table Storage 500 TB → 5 PiB per account** (storage-account max was raised). (e) Redis "1.2 TB (P5 Premium)" → "1.2 TB (P5 Premium, 10-shard cluster)" — a single P5 is 120 GB; 1.2 TB needs clustering. (f) **Single Server row was stale** — framed MySQL Single Server as merely "limited feature set"; both PostgreSQL Single Server and MySQL Single Server (retired Sept 2024) are now fully retired. (g) **"Azure Cognitive Search" → "Azure AI Search"** (renamed late 2023; 2 spots). **Item 6:** added a top-level selection decision tree (relational+ACID → SQL/MI/PostgreSQL/MySQL; non-relational → Synapse/ADX/Redis/AI Search/Cosmos) consolidating the workload table + per-service "when to use" lists — the strongest decision-tree candidate in the pass so far. **Items 5/7 no-ops:** inherently comparison-table-driven, no topology to diagram; scope not ambiguous. **Items 8-9:** tags `[azure, databases, decision-making, architecture, cloud-computing, scalability, practical]` → `[database-selection, azure-sql-database, cosmos-db, azure-cache-redis, synapse-analytics, polyglot-persistence, practical]`; description already names the services + axes, left alone. No sibling links (checked both forms). Linter clean. **Pre-flag row 22** (azure-sql-database): Hyperscale single-DB max is **128 TB** (elastic pool 100 TB); non-Hyperscale 4 TB — verify that guide matches. **Pre-flag row 23** (azure-cache-redis): P5 = 120 GB, 1.2 TB only via 10-shard clustering; also check whether it should mention **Azure Managed Redis** (the newer offering) and the Enterprise/Enterprise Flash tiers. **Pre-flag row 24** (azure-cosmos-db): session is the default of 5 consistency levels; periodic backup free up to 2 copies. |
| 21 | Database Services | azure-synapse-analytics.md | Complete | **Corrected (item 1):** (a) **DWU compute-node table was wrong and self-contradicting** — it showed DW30000c = 300 nodes while the prose (L57) correctly said "60 nodes maximum." The real mapping is nodes = DWU/500 (min 1): DW100c/500c = 1, DW1000c = 2, DW2000c = 4, DW5000c = 10, DW10000c = 20, DW30000c = 60. Replaced the unverifiable "vCores/Memory per node" columns (MS says node hardware is reference-only, not a sizing guarantee) with the correct node counts + a **distributions-per-node** column, which is what actually drives parallelism (60 fixed distributions spread over more nodes). (b) **Materialized views: guide said "manual refresh, incremental refresh not available" — the exact opposite of MS docs.** Synapse maintains MVs automatically and synchronously (base-table changes hit the view's delta store in the same transaction; tuple mover compacts). Rewrote the MV note. (c) **Pitfall 5 was built on the same MV misconception** ("schedule refreshes off-peak") — rewrote it around the real trade-off: synchronous maintenance adds write amplification to every base-table load, so over-creating MVs on write-heavy tables slows ingestion. (d) **Spark pool autoscale "0 to 600 nodes" → up to 200** (MS max). **Added (completeness):** a Microsoft Fabric callout — Fabric is Microsoft's announced successor and where new analytics investment is going (OneLake/Direct Lake/Fabric DW); Synapse stays GA/supported with no retirement date, but greenfield should evaluate Fabric first. Consequential for anyone choosing an analytics platform in 2026. **Item 5:** added a collocated-join-vs-shuffle diagram in Distribution Strategies (the MPP data-placement shape is the guide's headline design decision and was prose-only); the 3 pattern data-flow diagrams clear the bar and stay. **Items 8-9:** tags `[azure, databases, analytics, scalability, performance, cloud-computing, fundamentals]` → `[synapse-analytics, dedicated-sql-pool, apache-spark, data-warehouse, data-distribution, microsoft-fabric, fundamentals]`; description accurate, left alone. No sibling links (both forms). Linter clean. **Pre-flag row 42** (azure-synapse-analytics-query.md): must carry the same **Fabric successor** framing, and any MV/distribution claims must match (MVs are auto/synchronously maintained; 60 distributions; nodes = DWU/500). |
| 22 | Database Services | azure-sql-database.md | Complete | **Row 20's pre-flag confirmed correct** — Hyperscale 128 TB and non-Hyperscale 4 TB both already right here. **Corrected (item 1):** (a) **vCore max 80 → 128** for General Purpose and Business Critical (Gen5 now goes to 128; guide said 2-80). (b) **Serverless max 16 → 40 vCores**; auto-pause delay "default 1 hour / configurable 1-4 hours" → configurable **15 min to 7 days**; also noted serverless now exists on Hyperscale (without auto-pause), correcting "serverless is only available in General Purpose." (c) **DTU ranges:** Standard "100-3,000" → 10-3,000 (S0-S12), Premium "1,250-4,000" → 125-4,000 (P1-P15), Basic "5-100" → 5 DTUs. (d) **Hyperscale bullet "supports databases up to 100 GB without performance degradation"** was nonsensical for a 128 TB tier — replaced with a real differentiator (size-independent snapshot backups / near-instant restore); "up to 30 read replicas" → "30 named replicas". (e) **"Azure SQL Database supports only SQL authentication" is wrong** — it supports SQL auth *and* Microsoft Entra ID auth; only legacy on-prem Windows/Kerberos auth is unsupported. Rewrote the migration issue around the Entra ID path. (f) PITR "any moment in the last 35 days" → default 7 / up to 35 days + LTR to 10 years. **Verified correct, left alone:** built-in HA RPO 0 / RTO 30-120s, BC zone-redundant + readable replicas, GP 1 vCore ≈ 5.1 GB, geo-replication cost model. **Items 5-6 no-ops:** Hyperscale-architecture, elastic-pool, geo-replication, and 3 pattern diagrams all clear the bar; a migration decision tree already exists; tier selection served by per-tier "when to use" + Key Takeaways. Item 7 n/a (MI subnet-delegation vs DB Private-Endpoint scope already stated). **Items 8-9:** tags `[azure, databases, cloud-computing, scalability, reliability, performance, fundamentals]` → `[azure-sql-database, sql-managed-instance, hyperscale, elastic-pools, geo-replication, serverless, fundamentals]`; description accurate, left alone. No sibling links (both forms). Linter clean. |
| 23 | Database Services | azure-cache-redis.md | Complete | **Headline (item 1 completeness): the entire Azure Cache for Redis family is being retired and the guide never said so.** Enterprise/Enterprise Flash retire **2027-03-31**; Basic/Standard/Premium retire **2028-09-30**; **Azure Managed Redis** (on Redis Software from Redis Ltd.) is the single successor. Added a prominent retirement/AMR callout up top and noted the guide's concepts (clustering, persistence, patterns, geo-replication, connection mgmt) carry over to AMR while the tier limits below describe the classic offering. **Corrected:** (a) **Premium clustering "supports up to 500 shards" is wrong** → 10 shards GA (up to 30 preview; lower with replicas). (b) **RDB snapshot frequency "6, 15, 30, or 60 minutes" is wrong** — no 6-minute option; actual is 15/30/60 min or 6/12/24 hours (fixed in the Premium-capabilities list and the Data Persistence section). (c) **Premium sizes "6 GB to 1.2 TB"** conflated single-node with clustered (row 20 pre-flag) → "6-120 GB single-node (P1-P5); up to ~1.2 TB with 10-shard clustering." **Verified correct, left alone:** Basic/Standard 250 MB-53 GB, hash slots 16,384, SSL 6380/non-SSL 6379, Private Link all tiers. **Items 5-6 no-ops:** cache-aside/write-through/write-behind are sequential flows that read fine as numbered lists; clustering routing is conveyed by the CRC16 formula + prose; given the whole tier structure is retiring, a tier decision tree would be low-value. **Items 8-9:** tags `[azure, caching, performance, scalability, databases, cloud-computing, fundamentals]` → `[azure-cache-for-redis, azure-managed-redis, redis, cache-aside, clustering, geo-replication, fundamentals]`; description updated to name Azure Managed Redis as successor. No sibling links (both forms). Linter clean. **Cross-guide (retroactive):** added a one-line AMR-successor note to **row 20** (azure-database-selection.md), whose Redis tier table recommended "Standard tier is the baseline for production" and was steering readers to a retiring product. Any later guide that recommends a Redis tier must reference the AMR migration path. |
| 24 | Database Services | azure-cosmos-db.md | Complete | **Corrected (item 1):** (a) **Physical partition storage 10 GB → 50 GB**, and added the logical-partition ceiling the guide never stated (**20 GB** + 10,000 RU/s per logical partition — the hard limit that actually wedges a bad key). (b) **Integrated Cache was described as a client-side, per-connection, free/automatic cache — it's the opposite.** It's a **server-side** cache on a **dedicated gateway** (separately provisioned and billed per node-hour), shared across all clients routed through a node, session/eventual reads only, governed by `MaxIntegratedCacheStaleness` (default 5 min). Rewrote the whole section. (c) **Change feed "stream of (inserts, updates, deletes)" is wrong for the default mode** — latest-version mode surfaces inserts/updates only; a delete just vanishes from the feed. Deletes/TTL/intermediate versions need **all-versions-and-deletes** mode, which requires continuous backup. (d) **DynamoDB comparison "Global writes: not supported" is wrong** — DynamoDB global tables are multi-active (any region writes, LWW); reframed the row to the real distinction (Cosmos offers LWW *or* custom conflict resolution natively; Dynamo is LWW-only via a separate feature). Also softened the Dynamo "strong consistency requires application logic" cell to "strongly consistent reads per request, single-Region only." (e) **Throughput provisioning scope was called "account level" — there's no account-level throughput**; it's database-level (shared) or container-level (dedicated). (f) Pitfall 4 cost model refined: the N× reservation applies to *any* added region (read or write); multi-region writes add a separate higher per-RU rate on top. **Added (completeness):** session is the **default** consistency level (row 20 pre-flag); a **Backup and Restore** section — periodic (default, 4-hr backups, latest 2 free, restore via support ticket, lands in a new account) vs continuous PITR (7/30-day tiers, self-service, prerequisite for all-versions-and-deletes change feed), plus the restore constraints. **Item 5:** added a logical→physical partition mapping diagram contrasting even distribution vs. a hot partition (30k RU/s spread 3×10k vs. one partition capping at 10k with 20k stranded) — the guide's central concept, previously prose-only; the 3 pattern diagrams (global replication, multi-write merge, change-feed flow) clear the bar and stay. **Item 6:** added an API-selection decision tree (migration-source-driven, NoSQL default) consolidating the five per-API "use when" subsections. **Item 7:** logical/physical partition scope and database-vs-container throughput scope now stated up front. **Items 8-9:** tags `[azure, databases, distributed-systems, scalability, performance, cloud-computing, fundamentals]` → `[cosmos-db, consistency-levels, partition-keys, request-units, change-feed, global-distribution, fundamentals]`; description already names consistency/partitioning/multi-model API/global distribution, left alone. No sibling links (checked markdown + HTML forms). Linter clean. **Pre-flag rows 56/57** (serverless-architecture-patterns-azure, azure-functions-advanced): if either describes the Cosmos DB change feed, it must say latest-version mode captures inserts/updates only — deletes require all-versions-and-deletes mode + continuous backup — and must not describe the integrated cache as a client-side/free cache (it's a billed dedicated-gateway cache). |
| 25 | Application Integration & Messaging | azure-service-bus.md | Complete | **Corrected (item 1):** (a) **Default MaxDeliveryCount is 10, not 5** — the guide said "5 attempts / redelivered 5 times by default" in three spots (queue characteristics, DLQ intro, DLQ reasons). Fixed all. (b) **Default lock duration is 1 minute, not 30 seconds** (max 5 min); fixed the queue-characteristics line and reframed Pitfall 5 around renew-don't-pin. (c) **"Single active receiver per queue at any time" is wrong** — competing consumers receive concurrently; each *message* is locked to one consumer. Reworded. (d) **Tier limits were stale/wrong:** Basic "256 MB queue size" (that's a garbled message-size figure) → up to 5 GB/queue (80 GB partitioned), 256 KB max message; removed the "1 million / 12.5 million messages per day" quotas (no longer in the quotas doc — Basic/Standard are throttled at 1,000 ops/sec, billed per operation); Standard "1 GB" → up to 5 GB (80 GB partitioned); Premium "80 GB (up to 1,000 GB reserved)" → 80 GB/entity, namespace 1 TB per MU; "unlimited message throughput" → scales with messaging units (no fixed ops/sec cap); added Premium 100 MB large-message support (AMQP). (e) **Basic tier feature support was wrong** — the guide said Basic has "no scheduled delivery or dead-lettering"; both *are* supported on Basic. What Basic actually lacks: topics/subscriptions, sessions, duplicate detection, transactions. Rewrote the Basic bullet. (f) **"Browser-based session lock"** → broker-held, renewable, bounded by the entity lock duration (1-min default/5-min max); dropped the unverified "default 5 minutes" session-specific figure. (g) AWS-comparison duplicate-detection cell: "AWS = application-level" is incomplete — SQS FIFO has built-in content-based dedup (fixed 5-min window); reframed to contrast with Azure's configurable window (default 10 min, up to 7 days). **Verified correct, left alone:** duplicate detection default 10 min / max 7 days (guide was right; also confirmed Basic doesn't support it), max lock 5 min, transactions single-namespace, DLQ path/reasons, 4-hop auto-forward limit. **Item 5 (guide had zero diagrams):** added two — a topic filtered-fan-out diagram (one publish → independent copy per matching subscription, each with its own DLQ; the routing/branching structure that's the guide's core pub-sub differentiator) and a session-parallelism diagram (FIFO within a session, sessions concurrent across consumers; contrasts with a plain FIFO queue's head-of-line blocking). **Item 6:** added a tier-selection decision tree (tier guidance was spread across the tier bullets, Pitfall 6, and Key Takeaway 2). Item 7 n/a (namespace = regional container stated up front at L14). **Items 8-9:** tags `[azure, infrastructure, messaging, distributed-systems, reliability, scalability, practical]` → `[service-bus, topics-subscriptions, message-sessions, dead-letter-queue, duplicate-detection, scheduled-delivery, practical]`; description already names queues/topics/sessions/dead-lettering, left alone. No sibling links (checked markdown + HTML forms). Linter clean. **Cross-guide:** the SBMP protocol and the legacy SDK libraries (WindowsAzure.ServiceBus, Microsoft.Azure.ServiceBus, com.microsoft.azure.servicebus) retire **30 Sept 2026** — if any later messaging/integration guide (rows 26-28) references those SDKs or SBMP, flag the retirement. |
| 26 | Application Integration & Messaging | azure-event-hubs.md | Complete | **Corrected (item 1):** (a) **Standard-tier retention maxes at 7 days, not 90** — the tier table said Standard "1 day (up to 90)" and the AWS-comparison and Pitfall 4 implied 90 across the board. Only Premium/Dedicated reach 90; Basic is 1 day. Fixed all three spots. (b) **Premium max partitions is 100 per event hub, not 32** (tier table). (c) **Premium is 1-16 PUs, not "1-128"**, and a PU is **not** a fixed 1 MB/s throttle — Premium is capacity-based (throughput depends on payload/partitions/consumers; namespace scales to ~160 MB/s). Rewrote the PU bullet. (d) **Premium does not auto-scale/auto-inflate** — the guide claimed "automatically scales between min and max PUs" (PU changes are manual/dynamic, no downtime); auto-inflate is a **Standard**-only feature. Fixed the PU section, the tier-table Scaling row, the AWS-comparison auto-scaling row (which wrongly said "Auto-inflate (Premium tier)"), and Key Takeaway 7. (e) **Basic supports only the single default consumer group** — the entire consumer-groups section implied unlimited groups on any tier; added the per-tier limits (Basic 1, Standard 20, Premium 100, Dedicated 1,000). (f) **Kafka requires Standard+ (Basic has none)**, and **log compaction is now supported** on Standard+ — the guide listed "compacted topics" as unsupported (stale; the quotas page publishes compacted-entity sizes). Kept transactions as the real unsupported feature. (g) Per-tier **max event size** added (Basic 256 KB, Standard/Premium 1 MB, Dedicated 20 MB) — the guide asserted a flat "1 MB." (h) **Capture window** "every 1-5 minutes" → time window 1-15 min or size window 10-500 MB, whichever first. (i) Softened the Dedicated "200 MB/sec per CU" figure (2 spots) — MS publishes "no fixed per-CU rate; depends on factors"; reframed Dedicated as capacity-based, added the min-8-CU-for-AZ detail. **Verified correct, left alone:** Standard TU max 40, TU = 1 MB/s in / 2 MB/s out (1,000/4,096 events/s), Basic/Standard 32 partitions, Dedicated 1,024/event hub, Kinesis 24h/365-day retention, event hub name 256 chars. **Item 5:** added the guide's central missing structure — a partitioned-log diagram showing multiple consumer groups holding independent offsets into the same partitions (the "readers don't consume, they advance an offset; retention drops events, not consumption" model that distinguishes Event Hubs from a queue). The three integration-pattern diagrams show fan-out branches and stay. **Item 6:** the guide's "Tier selection decision tree" was actually a bulleted list — converted it into a real ASCII decision tree keyed off the corrected limits (Capture/Kafka/>1 group/>1-day retention gate Basic out; ≤7-day → Standard; isolation/90-day/dynamic-partitions → Premium; terabytes/day → Dedicated). **Items 8-9:** tags `[azure, infrastructure, messaging, distributed-systems, scalability, analytics, practical]` → `[event-hubs, event-streaming, partitions, consumer-groups, kafka, event-hubs-capture, practical]`; description already names partitions/consumer groups/capture/Kafka, left alone. No sibling links (both forms). Linter clean. **Pre-flag rows 41/44** (azure-data-factory, azure-data-architecture): if either describes Event Hubs retention or Kafka compatibility, Standard retention is **7 days** (not 90) and Kafka log compaction is now supported on Standard+. |
| 27 | Application Integration & Messaging | azure-event-grid.md | Complete | **Highest error density in the pass so far, including one fabricated feature.** **Headline gap (item 1 completeness): the guide never mentioned Event Grid namespaces / the standard tier**, so it described a two-tier product as if only the basic tier existed. Added a tier comparison up front and a full Pull Delivery section (namespace topics, receive/acknowledge/release/reject/renew-lock with lock tokens, 7-day retention, MQTT v3.1.1/v5, 40 MB/s ingress / 80 MB/s egress, private-link on the consume path) plus what standard tier *cannot* do (no system topics, no partner events, no domains, push to Event Hubs only, CloudEvents JSON only) since the tiers are not a superset relationship. **Corrected:** (a) **Dead-letter destination is a Storage blob container, not "a Storage Queue or Service Bus Queue"** — the error ran through the DLQ section, the config block, Pitfall 3, all of Pitfall 6, and Key Takeaway 10. Rewrote around the real behavior: off by default, ~5-min write delay, **event dropped if the container is unreachable for 4 hours**, dropped if the destination isn't found, `deadLetterReason`/`lastDeliveryOutcome` metadata, managed identity needs a storage RBAC role. Pitfall 6 ("queue fills up, size it appropriately") was wrong at the premise and was rewritten around lifecycle policy + alerting on dead-letter *rate* + building the replay path (there is no built-in replay). (b) **"Strict ordering per subject" does not exist.** The guide had a whole subsection, a config block ("Ordered message delivery = Enabled"), and an AWS-comparison row for a feature Event Grid has never had — MS states flatly that order isn't guaranteed. Replaced with the real guidance (sequencer/version fields, idempotent+commutative handlers, read-current-state, or use Service Bus sessions / Event Hubs partitions when order is a correctness requirement). (c) **Retry schedule was wrong** — "first retry immediately, then 2^n seconds capped at 3600" → Event Grid waits **30 s** for a response, then 10 s, 30 s, 1 m, 5 m, 10 m, 30 m, 1 h, 3 h, 6 h, every 12 h to 24 h. Added the TTL-vs-max-attempts interaction (first to expire wins; MS's own example shows max-attempts=10 is a no-op at a 30-min TTL) and **delayed delivery / probation**, which is why a recovered endpoint doesn't immediately drain its backlog. (d) **Success/failure codes:** only 200-204 succeed, and **400/403/413 are never retried** — a handler returning 400 on an unparseable payload discards the event on attempt one. Added the full retry-behavior table. (e) **Throughput "1 million events/sec per region" → 5,000 events or 5 MB/s per topic**; **"10,000 subscriptions per topic (soft limit)" → 500, not increasable** (100 at Azure-subscription scope). Rebuilt the perf table as basic-vs-standard with per-region framing. (f) **Event Grid schema is not "a superset of CloudEvents"** — MS calls it proprietary and non-extensible and recommends **CloudEvents** as the default, including for Azure-only systems. Added the asymmetric conversion matrix (CloudEvents input can only emit CloudEvents), which constrains migration order, plus the missing `metadataVersion` field and the required/stamped property split. (g) **System topic source list was half wrong** — Cosmos DB, Azure SQL Database, Synapse, and Web PubSub are **not** Event Grid sources (Cosmos's change feed is the mechanism); replaced with the real list, and fixed the Built-In Integrations table (dropped Cosmos DB and Azure Data Share). (h) **Handler table listed two destinations that don't exist**: "Azure Storage Blob (event archival)" and "Power Automate flows". Blob containers are only ever a dead-letter target; Power Automate uses its own connector. Logic Apps and Automation reframed as the webhook path; added Relay hybrid connections, Azure Monitor alerts (Key Vault source only), and namespace-topic handlers. (i) **"Pull handlers (Service Bus, Event Hubs, Storage Queues)" is a category error** — all basic-tier handlers are push; those three are buffers Event Grid pushes *into*, and each delivery is a billed, retryable operation. (j) **Custom topics don't support custom domain names** (that's a namespace feature needing a Key Vault cert + TXT proof). (k) Pitfall 2's "default 60 seconds" HTTP timeout → **30 seconds**. (l) Pitfall 4 rewritten: validation applies to webhooks only (Logic Apps connector / Automation webhooks / Functions with the **Event Grid trigger** are automatic, an HTTP-triggered Function is not), sync handshake needs **HTTP 200** (202 rejected) in 30 s, manual `validationUrl` valid **10 min** on **port 553**, self-signed certs unsupported, and CloudEvents output uses the **HTTP OPTIONS** abuse-protection handshake instead. (m) **Domain-vs-topics table cost row was wrong** — Event Grid has no per-topic charge, both bill per operation; the real trade is 100,000 topics/domain vs 100 custom topics per subscription per region, against a *shared* 5 MB/s domain ingress budget. Added that domain topics are **auto-managed** and that **domains don't broadcast** (every event names one target topic; N tenants means N copies). (n) **Cost model row** "per million events delivered" → per **operation** (ingress + each delivery attempt + each filter match + each dead-letter write), metered in **64 KB units**, first 100K free, ~$0.60/M in East US (Retail Prices API). **Added (completeness):** advanced-filter limits that actually bite (25 filters and 25 values total, 512 chars, OR-within/AND-across, case-insensitive comparisons, no arrays-of-objects, **keys containing a dot are unfilterable**, and negative operators match when the key is absent); subject filters are prefix/suffix only; subscription expiration; the 5,000-event publish batch. **Item 7:** topic scope stated up front (regional, 100/subscription/region, non-increasable), plus system-topic placement (same region + subscription as source, global sources go to `global`, subscription-scope lands in `Default-EventGrid` in **West US 2**). **Item 5 (guide had zero real diagrams; the one ASCII block was a flat 1→3 label fan):** added four — per-subscription fan-out with independent retry/dead-letter state, push-vs-pull delivery paths (the two-shapes case, and the guide's central tier distinction), the delivery-outcome flow (200-204 / never-retried / retry ladder / TTL-or-attempts / dead-letter-or-drop), and domain routing by `source` with per-topic RBAC and the domain-scope subscription. **Item 6:** added a tier-and-topic-type decision tree (MQTT → namespace; pull/private-link/>5 MB/s/>1-day retention → namespace; else basic, then system vs partner vs domain vs custom topic). **Items 8-9:** tags `[azure, infrastructure, messaging, distributed-systems, scalability, architecture, practical]` → `[event-grid, cloudevents, event-subscriptions, event-domains, dead-lettering, pull-delivery, practical]`; description rewritten (it named neither tier nor pull delivery). No sibling links (checked markdown + HTML forms). Linter clean. **Cross-guide:** Event Grid's dead-letter destination is a **blob container**, never a queue — any later guide saying otherwise is wrong. **Pre-flag row 28** (azure-logic-apps): Logic Apps is an Event Grid destination via the Event Grid connector over the webhook path, not a native destination type. **Pre-flag row 35** (azure-key-vault): Key Vault near-expiry/expiry events via an Event Grid system topic are the supported rotation trigger, and Azure Monitor alerts are an Event Grid handler **only** when the source is Key Vault. **Pre-flag rows 41/44** (data-factory, data-architecture): Cosmos DB does not publish to Event Grid; if either guide claims an Event Grid integration for Cosmos, it means the change feed. **Pre-flag rows 56/57** (serverless-architecture-patterns-azure, azure-functions-advanced): a Function subscribed to Event Grid needs the **Event Grid trigger** to get automatic endpoint validation (an HTTP trigger does not), Event Grid delivery is unordered at-least-once with no ordering option, and any Event Grid discussion must not treat basic tier as the whole product. |
| 28 | Application Integration & Messaging | azure-logic-apps.md | Complete | **First row where the code samples were the worst of it.** Covers Logic Apps *and* Durable Functions, and four of the five C# samples had errors that would not compile or would not do what the prose claimed. **Corrected (code):** (a) **Pattern 4's approval sample used a `WaitForExternalEvent<bool>(name, timer)` overload that does not exist** — the API takes only an event name and waits indefinitely. Rewrote with the documented `CancellationTokenSource` + `CreateTimer` + `Task.WhenAny` race, and added the trap that omitting `timeoutCts.Cancel()` leaves an approved orchestration Running until the deadline. (b) **The entity sample had three wrong APIs**: `[EntityFunctionInput]` → **`[EntityTrigger]`**, `ctx.State<int>()` → **`ctx.GetState<int>()`**, `ctx.SetResult(...)` → **`ctx.Return(...)`**. (c) **Pattern 1 wouldn't compile** — non-generic `CallActivityAsync` returns `Task`, so `var x = await ...` binds nothing, and the method was declared `Task` while returning a value. Fixed to typed `CallActivityAsync<T>` / `Task<T>` (same fix in Pattern 2). (d) **Pattern 3's return type was wrong**: `CreateCheckStatusResponse(HttpRequestMessage, ...)` returns `HttpResponseMessage`, not `IActionResult`; also replaced the legacy `ReadAsAsync<string>()`. **Corrected (facts):** (e) **"400+ connectors" → 1,400+** (4 spots). (f) **Logic Apps "Long-running support: Unlimited" → 90 days max run duration** (Consumption and Standard stateful; stateless is 5 min). (g) **"Workflow timeout: default 30 days" and "Action timeout: default 1 hour" are both wrong** — run duration is 90 days, and the action limit that actually bites is the **outbound HTTP timeout: 120 s on Consumption / 225 s on Standard**. The 1 hour is the `Until` loop timeout. Replaced with a per-tier limits table, plus the retention-must-exceed-run-duration trap. (h) **Pitfall 6's "default 1,000 concurrent executions per region per subscription" is wrong** — trigger concurrency is *unlimited* by default, and **turning it on is irreversible** (caps at 25 default on Consumption / 100 on Standard, 100 max). The real ceiling is **100,000 action executions per 5-minute rolling interval** (300,000 in preview high-throughput mode), plus ~2,500 concurrent outbound calls and 6 GB content per 5 min. Rewrote the pitfall around both surprises. (i) **Durable Functions state "backed by Azure Table Storage or SQL Database"** understates and misses the current recommendation — added a storage-provider table: **Durable Task Scheduler (Azure-managed, GA, Microsoft's recommended option)**, Azure Storage, MSSQL, and **Netherite (support ends 2028-03-31)**, plus the fact that **there is no migration path between providers** and that Flex Consumption supports only DTS + Azure Storage. (j) **Standard tier hosting was "App Service Plan or container"** — it's **Workflow Service Plan**, **ASE v3 (Windows plans only)**, or **hybrid deployment** (Logic Apps runtime on your own infra as an Azure Container Apps extension). (k) **"containerized Consumption" does not exist** (built-in connector section). (l) Pitfall 3's heading ("Orchestrator Functions Calling Activity Functions Directly") contradicted its own body, and the underlying claim was framed as a framework prohibition — reframed around what actually goes wrong (uncheckpointed work, retry policies not applying, opaque run history) and pointed at sub-orchestrations. **Added (completeness):** the **Consumption = one workflow per resource / Standard = many** structural split, which drives most of the tier differences and was absent; **Standard's built-in connectors for Service Bus, Event Hubs, Blob Storage, SQL, Cosmos DB, SFTP** (the guide's built-in list was HTTP/Batch/Request/Schedule/Workflow, which badly understates the tier's cost and throughput advantage and is invisible in the designer); **sub-orchestrations**; **task hubs** (item 7 scope: instance IDs are unique per task hub, and two deployments sharing a storage account without distinct hub names collide); entities **run operations serially** and the signal-vs-call asymmetry (orchestrators can call, clients can only signal + read possibly-stale persisted state, entities can only signal); **`LockAsync` critical sections** with the no-nesting/no-suborchestration/no-rollback rules; **orchestrators cannot do I/O at all** (previously only "must be deterministic"), with `CallHttpAsync` as the sanctioned exception and replay-safe logging; **a failed orchestration cannot be retried**; **external events and Logic Apps messages are both at-least-once** and need idempotence; stateless workflows' real constraints (5-min duration, synchronous only, 64 KB payload guidance, manual resubmit after interruption, and no run history to debug with); Consumption bills nothing while an orchestrator awaits a timer or event; Durable Functions bills **storage transactions** on top of executions. **Item 5 (guide had zero diagrams):** added two — the **orchestrator replay/event-sourcing timeline** (attempt 1/2/3 showing every prior line re-running from history), which is the guide's central concept and was asserted twice without ever being shown, and a **saga compensation flow** showing which compensations fire at each failure point. **Item 6:** added a combined platform-and-tier decision tree; the guide had a Decision Framework and a Selection Matrix for Logic-Apps-vs-Durable-Functions but nothing that reached the Consumption-vs-Standard choice, which lived in a different section. **Items 8-9:** tags `[azure, infrastructure, distributed-systems, automation, integration, scalability, practical]` → `[logic-apps, durable-functions, workflow-orchestration, durable-entities, saga-pattern, connectors, practical]`; description updated to name hosting tiers, the replay model, and compensation. Also fixed a duplicate "10." in Key Takeaways. No sibling links (checked both forms). Linter clean. **Cross-guide:** the Durable Functions docs have moved to `learn.microsoft.com/azure/durable-task/` and are now zone-pivoted between **Durable Functions** and the standalone **Durable Task SDKs** — check which pivot a claim came from (this is the tier/zone-pivot gotcha in a new place). **Pre-flag rows 56/57** (serverless-architecture-patterns-azure, azure-functions-advanced): any Durable Functions content must use `[EntityTrigger]`/`GetState`/`Return`, must race `WaitForExternalEvent` against a durable timer rather than passing one to it, and must name **Durable Task Scheduler** as the recommended backend (Netherite retires 2028-03-31, no cross-provider migration). **Pre-flag row 40** (azure-cost-management): Logic Apps Consumption bills per action *and* per managed-connector call, which is the pairing that surprises people. |
| 29 | Infrastructure as Code | azure-bicep-fundamentals.md | Complete | **The guide taught two things Microsoft actively warns against, and one of them was its own flagship example.** No Bicep CLI available locally, so samples were checked against the API/schema docs rather than compiled. **Corrected (the two dangerous ones):** (a) **The Outputs section's headline example was Microsoft's own documented linter-*failure* case** — it built a storage connection string with `listKeys(...)` in an output. Outputs live in the deployment history, readable by anyone with resource-group read access, and `outputs-should-not-contain-secrets` exists specifically to catch it. Replaced with outputting the resource ID, and added the secrets-in-outputs discussion. Compounding it, **Pitfall 4's fix ("Mark sensitive outputs as sensitive") describes a feature that does not exist** — `@secure()` is parameters-only and **there is no secure-output decorator**, so the fix is structural, not a keyword. Rewrote around output-the-ID-and-fetch-at-use, `getSecret`, managed identity, and `#disable-next-line` for genuine false positives. (b) **The implicit-dependency example declared a subnet as a standalone `Microsoft.Network/virtualNetworks/subnets` child resource**, which MS's deployment-modes doc explicitly says not to do: redeploying the VNet reapplies its full property set, so a VNet declared without a `subnets` property has no subnets, and child-declared subnets get wiped. Rewrote the example around the VNet's `subnets` property and added the `Microsoft.Web/sites/config` sibling case. **Corrected (deployment modes, now largely deprecated advice):** (c) **Complete mode is being retired** — MS's current guidance on both the incremental and complete sections is to use **deployment stacks** for deletions, and states complete mode "will be gradually deprecated." The guide presented it as a co-equal option with a neutral "when to use" list. Reframed as legacy, and added the edges it never mentioned: **resource-group scope only** (subscription deployments don't support it, nested/linked must be incremental), locks block it, some child resources survive, copy loops are dangerous, and `condition: false` resources are now deleted. (d) **"Incremental mode = partial templates" is the guide's most consequential error.** Incremental describes the *resource group*, not the resource: within a resource that is in the template, **every omitted property resets to its default**. Added a before/after table showing subnets and tags silently disappearing, plus a new **Pitfall 7** for it, since this is the mechanism behind (b). (e) **What-if lists seven change types, not four**, and the guide's definition of **Ignore was wrong** ("resources outside the template scope" — it actually means untracked resources that will be left alone, the normal incremental outcome). Added `NoChange`, `NoEffect`, `Deploy`, and a "reading past the noise" subsection: what-if cannot evaluate `utcNow()`, `newGuid()`, `listKeys()`, secure params, or `reference()`, so those always report as changes, and nested-template expansion limits (500/800/5 min) silently downgrade the rest to Ignore. Also noted what-if works at all four scopes and `--confirm-with-what-if`. **Corrected (samples):** (f) **management-group `policyAssignment` set `properties.scope`**, which is read-only and derived from the deployment target; removed, and explained where assignment scope actually comes from. (g) **tenant-scope `roleDefinition` omitted `assignableScopes`**, which is required. (h) Variables named `length` and `substring` shadow built-in functions; renamed, with a caution. (i) Single-line comma-separated array/object literals rewritten in the conventional newline form. (j) `listKeys(id, apiVersion)` → the idiomatic `storageAccount.listKeys()`. (k) The **"Explicit Dependencies" section demonstrated implicit dependencies and never showed `dependsOn`** (same heading/body mismatch class as row 28's Pitfall 3); replaced with a real `dependsOn` example and a caution that unnecessary entries serialize work ARM would parallelize. **Added (completeness, all absent and all fundamental):** **`@secure()` parameters**; **`.bicepparam` parameter files** (the guide only showed inline `--parameters key=value`) with the plain-text warning and the Key Vault `getSecret` pattern; the **`existing` keyword**, which is how templates compose across ownership boundaries; **loops and conditions on resources** (previously shown for modules only), including that a false-condition resource yields null rather than a compile error; the **Bicep linter and `bicepconfig.json`**; and a full **Deployment Stacks** section (`actionOnUnmanage` detach/deleteResources/deleteAll, deny settings none/denyDelete/denyWriteAndDelete, stack-above-resources placement, control-plane-only, explicit-resources-only, the silent **5-principal** exclusion cap, stack-out-of-sync). Also added Terraform's state file to the comparison table (the real operational difference, previously absent) and corrected its "Basic type support" row. **Item 5 (guide had zero diagrams):** added two — the **toolchain pipeline** showing the three separate places mistakes get caught (compiler/lint, ARM preflight, what-if), and the **incremental-mode property-reset table** that makes (d) concrete. **Item 6:** added a decision tree for "how do I handle resources not in my template," which is exactly the question complete mode and deployment stacks both answer. **Items 8-9:** tags `[azure, infrastructure, cloud-computing, devops, automation, fundamentals]` → `[bicep, arm-templates, infrastructure-as-code, bicep-modules, deployment-stacks, what-if, fundamentals]`; description updated to name scopes, deployment modes, stacks, and what-if. No sibling links (both forms). Linter clean. **Cross-guide:** deployment stacks are now confirmed from a third direction — row 3 found them replacing Blueprints deny assignments, row 36 is pre-flagged for the Blueprints section, and MS is now routing complete-mode users to them too. **Pre-flag rows 30/31** (bicep-advanced, arm-templates): row 31 especially, since an ARM JSON guide will describe deployment modes and may carry the same "incremental is partial" error and the same complete-mode framing; also verify neither puts `listKeys` in an output. **Pre-flag row 36** (policy-governance): the `policyAssignment` scope correction applies there too if it shows assignment templates. |
| 30 | Infrastructure as Code | azure-bicep-advanced.md | Complete | **Second fabricated-code finding of the pass, and the densest set of compile errors yet.** **Corrected (invalid code):** (a) **The Kubernetes extension sample was invented syntax.** `import kubernetes as k8s` plus `resource namespace = k8s.core.v1.Namespace.new('kube-system')` is not Bicep in any version. The real form is `extension kubernetes with { namespace, kubeConfig } as k8s` with normal `resource 'core/Service@v1'` declarations, it still requires `experimentalFeaturesEnabled.extensibility` in `bicepconfig.json`, and it **does not support private clusters** (which rules it out for most production AKS). Rewrote with the parent/module kubeConfig-as-`@secure()`-param pattern from the docs. Also **"providers include Kubernetes, Docker, and HTTP" is wrong** — the term is *extensions*, and Docker isn't one; replaced with Microsoft Graph. (b) **`@allowed` is permitted only on `param` statements**, and the guide used it twice inside `type` declarations. MS's UDT doc is explicit: use union type syntax (`'a' | 'b'`) in a type. Rewrote both types, and added `@sealed()`. (c) **`@minValue(0)`/`@maxValue(1)` applied to a `bool`** — those decorators are int-only. (d) **User-defined functions omitted the required return type** (`func name(args) <type> => expr`) in both examples; also documented the real restrictions (no `reference()`, no `list*`, no parameter defaults) and refactored the sample to take the RG id as a `salt` parameter rather than calling scope functions inside the function. (e) **Loop variable order reversed in two outputs** — Bicep is `for (item, index)`, so `for (i, location) in locations` binds `i` to the string and `location` to the int; `storageAccounts[i]` then indexes with a string. Fixed both and switched the NIC outputs to index-based iteration. (f) **`diagnosticSettings` sample had no `scope`** (it's an extension resource, so it's required) and used **`retentionPolicy`, which is retired**; retention now belongs to the destination. Also bumped the 2017 preview API version. (g) A symbolic name `resourceGroup` **shadowed the `resourceGroup()` function** (same class as row 29's `length`). (h) The NIC loop referenced an undeclared `vnet`. (i) Registry module output name didn't match the module's actual output. **Corrected (facts):** (j) **"Linked Modules live in storage accounts"** — a Bicep module is a local path or a registry reference, full stop; storage-account URLs are ARM linked templates, a different mechanism. (k) **Deployment stacks section had two invented capabilities**, "deny deletion if resources have dependencies" and "dependency tracking"; neither is a thing stacks do. Replaced with the real `actionOnUnmanage` and `deny-settings-mode` values plus the enterprise constraints (control-plane only, explicit resources only, the silent 5-principal cap, stack-above-resources placement, stack-out-of-sync). (l) **What-if change types listed 5 of 7** (missing `NoEffect`, `Deploy`); aligned with row 29 and added the noise/expansion-limit caveats. (m) Comparison table said Bicep state management is "Implicit (stored in Azure deployments)" — Bicep has no state; aligned with row 29, and fixed the "Drift detection: Manual" row. (n) **GitHub Actions workflow used `actions/checkout@v3`, `azure/login@v1`, and a long-lived `AZURE_CREDENTIALS` secret**; updated to v4/v2 with **OIDC federated credentials** and `id-token: write`, added the missing `needs:` on the what-if job, and added least-privilege-per-stage and no-rollback-command notes. **Added (completeness):** **template specs — promised in the guide's own description and never mentioned** (item 8 catch), with the registry-vs-spec distinction (building block you compose vs finished deployable governed by RBAC) and the `ts:` module scheme; **`bicepconfig.json` module aliases** (`br/alias:`); **every module is a nested deployment**, with the consequences that actually bite (deployment names unique per scope, 64-char cap, what-if's 500-template expansion limit, output chaining serializing work); **`import`/`@export()`** (the guide exported types and then used them cross-file with no import statement); **`.bicepparam` files** with `extends` for shared values, replacing the JSON-only parameter section, per row 29; **`resourceInput<>`/`resourceOutput<>` resource-derived types**; **`@discriminator()` tagged unions**; ACR RBAC (AcrPush/AcrPull) and immutable tags for registries. **Item 5 (guide had zero diagrams):** added the multi-scope module deployment diagram showing how `scope:` turns each module into a nested deployment at a different scope, which the Mixed Scope section described in prose only. **Item 6:** added a module-sharing decision tree (local path vs registry vs template spec), consolidating a choice previously split across two sections. **Items 8-9:** tags `[azure, infrastructure, cloud-computing, devops, automation, advanced, design-patterns]` → `[bicep, bicep-modules, user-defined-types, bicep-registry, template-specs, deployment-stacks, advanced]`; description rewritten (it promised template specs the guide didn't cover, and omitted extensions). Fixed a duplicate "10." in Key Takeaways. No sibling links (both forms). Linter clean. **Verified, no correction needed:** `az bicep lint` is a real command (checked the Azure CLI command table directly, alongside `build-params`, `format`, `snapshot`, `run`). **Pre-flag row 31** (arm-templates): verify its deployment-mode framing against row 29's findings, and check whether it repeats the "linked templates in storage accounts" conflation, which is *correct* for ARM JSON and wrong only for Bicep. **Pre-flag row 47** (github-actions-azure): the OIDC-over-stored-secret correction applies there directly, as does `azure/login@v2`. |
| 31 | Infrastructure as Code | azure-arm-templates.md | Not started | |
| 32 | Security & Compliance | azure-defender-sentinel.md | Not started | |
| 33 | Security & Compliance | azure-monitor-diagnostic-settings.md | Not started | |
| 34 | Security & Compliance | azure-firewall-ddos.md | Not started | |
| 35 | Security & Compliance | azure-key-vault.md | Not started | |
| 36 | Security & Compliance | azure-policy-governance.md | Not started | **Pre-flagged from row 3:** has a full "Azure Blueprints" section (~L190-247), recommends Blueprints for new subscriptions (L390), and names it in the description and Key Takeaways (L554). Blueprints begins phased retirement 2026-07-31 and is fully retired 2027-01-31; replacements are deployment stacks (assign/deploy/lifecycle/deny) + template specs (store/version). This section needs rewriting, not tweaking — do not just append a deprecation note to advice that says "use this." |
| 37 | Management & Governance | azure-monitor.md | Not started | |
| 38 | Management & Governance | azure-application-insights.md | Not started | |
| 39 | Management & Governance | azure-automation-arc.md | Not started | |
| 40 | Management & Governance | azure-cost-management.md | Not started | |
| 41 | Analytics & Data Processing | azure-data-factory.md | Not started | |
| 42 | Analytics & Data Processing | azure-synapse-analytics-query.md | Not started | |
| 43 | Analytics & Data Processing | azure-power-bi.md | Not started | |
| 44 | Analytics & Data Processing | azure-data-architecture.md | Not started | |
| 45 | Developer Tools & CI/CD | azure-devops-pipelines.md | Not started | |
| 46 | Developer Tools & CI/CD | azure-devops-repos-artifacts.md | Not started | |
| 47 | Developer Tools & CI/CD | github-actions-azure.md | Not started | |
| 48 | Machine Learning & AI | azure-machine-learning.md | Not started | |
| 49 | Machine Learning & AI | azure-ai-vision.md | Not started | |
| 50 | Machine Learning & AI | azure-ai-language.md | Not started | |
| 51 | Machine Learning & AI | azure-ai-speech.md | Not started | |
| 52 | Machine Learning & AI | azure-ai-service-selection.md | Not started | |
| 53 | Migration & Hybrid Cloud | azure-migration-strategy.md | Not started | |
| 54 | Migration & Hybrid Cloud | azure-migrate-services.md | Not started | |
| 55 | Migration & Hybrid Cloud | azure-hybrid-cloud-architecture.md | Not started | |
| 56 | Serverless Architecture | serverless-architecture-patterns-azure.md | Not started | |
| 57 | Serverless Architecture | azure-functions-advanced.md | Not started | |
| 58 | Container Orchestration (Advanced) | azure-acr-container-security.md | Not started | |
| 59 | Container Orchestration (Advanced) | advanced-container-patterns-azure.md | Not started | |
| 60 | Architecture Patterns (Advanced) | multi-region-architecture-azure.md | Not started | |
| 61 | Architecture Patterns (Advanced) | disaster-recovery-azure.md | Not started | |
